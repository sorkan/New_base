1. The SCC_EXERCISE1 builds a random graph given options at the command line and stores it in a sqllite3 database (file based)
2. The SCC_EXERCISE2 reads from the sqllite3 database created by the above script into an hash and attempts 
   to find the SCC (Strongle Connected Components) of the loaded graph.